This project demonstrates the Retrieval-Augmented Generation (RAG) workflow using Python 3.13 and the LangChain framework.
To run the project, please use

```markdown
python -m venv .venv
source .venv/bin/activate
pip install -r dependence.txt
python3.13 main.py
